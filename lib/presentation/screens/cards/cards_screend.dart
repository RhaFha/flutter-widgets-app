import 'package:flutter/material.dart';

class CardsScreend extends StatelessWidget {

  static const String name = 'cards_screen';

  const CardsScreend({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cards Screeend'),
      ),
      body: const Placeholder(),
    );
  }
}